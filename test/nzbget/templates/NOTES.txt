1. Create a port-forward to the nzbget pod:
   kubectl port-forward -n {{ .Release.Namespace }} $(kubectl get pod -n {{ .Release.Namespace }} --selector=app={{ template "nzbget.name" . }} -o jsonpath='{.items...metadata.name}') 6789:6789

   Open browser to UI:

   open http://localhost:6789
